{
  "name": "@hoopla-pipeline/design-system-server",
  "version": "1.0.0",
  "description": "MCP server exposing design tokens, component specifications, screen layouts, WCAG annotations, and Figma Make prompt generation to pipeline agents",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
