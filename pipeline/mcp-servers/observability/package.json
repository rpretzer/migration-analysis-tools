{
  "name": "@hoopla-pipeline/observability-server",
  "version": "1.0.0",
  "description": "MCP server for append-only observability event log — logs agent decisions, reads traces, and surfaces dashboard metrics",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
